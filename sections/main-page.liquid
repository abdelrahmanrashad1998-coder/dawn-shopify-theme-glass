{{ 'section-main-page.css' | asset_url | stylesheet_tag }}

{%- style -%}
  .section-{{ section.id }}-padding {
    padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
    padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
  }

  @media screen and (min-width: 750px) {
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top }}px;
      padding-bottom: {{ section.settings.padding_bottom }}px;
    }
  }

  {%- if section.settings.page_background_image != blank -%}
    body {
      --background-image: url('{{ section.settings.page_background_image | image_url }}');
      --background-opacity: {{ section.settings.page_background_opacity | divided_by: 100.0 }};
      --background-size: {{ section.settings.page_background_size | default: 'cover' }};
      --background-position: {{ section.settings.page_background_position | default: 'center' }};
    }
  {%- endif -%}
{%- endstyle -%}

{% comment %} Check if this is a policy page - Multiple Detection Methods {% endcomment %}
{%- assign is_policy_page = false -%}

<!-- Method 1: Direct URL comparison -->
{%- for policy in shop.policies -%}
  {%- if policy.url == page.url -%}
    {%- assign is_policy_page = true -%}
    {%- break -%}
  {%- endif -%}
{%- endfor -%}

<!-- Method 2: Check for /policies/ in URL -->
{%- if page.url contains '/policies/' -%}
  {%- assign is_policy_page = true -%}
{%- endif -%}

<!-- Method 3: Check if page title matches policy titles -->
{%- assign policy_titles = '' -%}
{%- for policy in shop.policies -%}
  {%- assign policy_titles = policy_titles | append: '|' | append: policy.title | append: '|' -%}
{%- endfor -%}
{%- if policy_titles contains page.title -%}
  {%- assign is_policy_page = true -%}
{%- endif -%}

<!-- Method 4: Check page handle vs policy handles -->
{%- for policy in shop.policies -%}
  {%- if page.handle == policy.handle -%}
    {%- assign is_policy_page = true -%}
    {%- break -%}
  {%- endif -%}
{%- endfor -%}

<!-- Method 5: Check if URL contains policy handle -->
{%- for policy in shop.policies -%}
  {%- if page.url contains policy.handle -%}
    {%- assign is_policy_page = true -%}
    {%- break -%}
  {%- endif -%}
{%- endfor -%}

<!-- DEBUG: Show visual indicator for policy page -->
{%- if is_policy_page -%}
  <div style="background: red; color: white; padding: 20px; margin: 20px; font-size: 18px; text-align: center; font-weight: bold;">
    🔥 POLICY PAGE DETECTED - GLASS EFFECT SHOULD BE VISIBLE 🔥<br>
    Policy: {{ page.title }} | URL: {{ page.url }}
  </div>
{%- endif -%}

<!-- DEBUG: Super aggressive debugging -->
<div style="background: orange; color: black; padding: 20px; margin: 20px; font-size: 16px; text-align: left; border: 3px solid red;">
  🔥 AGGRESSIVE DEBUG 🔥<br><br>
  
  <strong>1. PAGE INFO:</strong><br>
  Current URL: <span style="background: yellow;">{{ page.url }}</span><br>
  Page Title: <span style="background: yellow;">{{ page.title }}</span><br>
  Page Handle: <span style="background: yellow;">{{ page.handle }}</span><br>
  Page ID: <span style="background: yellow;">{{ page.id }}</span><br><br>
  
  <strong>2. METHOD TESTING:</strong><br>
  Method 1 (Direct): <span style="background: yellow;">{{ page.url }} == policy.url</span><br>
  Method 2 (Contains): <span style="background: yellow;">{{ page.url contains '/policies/' }}</span><br>
  Method 3 (Title): <span style="background: yellow;">{{ policy_titles }}</span><br>
  Final Result: <span style="background: yellow;">{{ is_policy_page }}</span><br><br>
  
  <strong>3. ALL SHOP POLICIES:</strong><br>
  {% for policy in shop.policies %}
    {%- if policy != blank -%}
      → Policy: <strong>{{ policy.title }}</strong><br>
      → URL: <span style="background: yellow;">{{ policy.url }}</span><br>
      → Handle: <span style="background: yellow;">{{ policy.handle }}</span><br>
      → ID: <span style="background: yellow;">{{ policy.id }}</span><br>
      → Matches?: <span style="background: {% if policy.url == page.url %}green{% else %}red{% endif %};">{{ policy.url == page.url }}</span><br><br>
    {%- endif -%}
  {% endfor %}
  
  <strong>4. MANUAL TESTS:</strong><br>
  Current URL length: {{ page.url.size }}<br>
  URL starts with '/policies': {{ page.url | slice: 0, 9 }}<br>
  Last 20 chars: {{ page.url | slice: -20, 20 }}
</div>

<div class="page-width page-width--narrow section-{{ section.id }}-padding">
  <div class="page-content-glass{% if is_policy_page %} policy-page-glass{% endif %}{% if settings.animations_reveal_on_scroll %} scroll-trigger animate--fade-in{% endif %}">
    <h1 class="main-page-title page-title h0{% if is_policy_page %} policy-title{% endif %}{% if settings.animations_reveal_on_scroll %} animate--slide-in{% endif %}">
      {{ page.title | escape }}
    </h1>
    <div class="rte{% if is_policy_page %} policy-content{% endif %}{% if settings.animations_reveal_on_scroll %} animate--slide-in{% endif %}">
      {{ page.content }}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "t:sections.main-page.name",
  "tag": "section",
  "class": "section",
  "settings": [
    {
      "type": "header",
      "content": "Background Image"
    },
    {
      "type": "image_picker",
      "id": "page_background_image",
      "label": "Page Background Image",
      "info": "Upload an image to use as a fixed background for this page. This will override the global background image."
    },
    {
      "type": "range",
      "id": "page_background_opacity",
      "min": 0,
      "max": 100,
      "step": 5,
      "default": 100,
      "unit": "%",
      "label": "Background Image Opacity"
    },
    {
      "type": "select",
      "id": "page_background_size",
      "options": [
        {
          "value": "cover",
          "label": "Cover"
        },
        {
          "value": "contain",
          "label": "Contain"
        },
        {
          "value": "auto",
          "label": "Auto"
        }
      ],
      "default": "cover",
      "label": "Background Size"
    },
    {
      "type": "select",
      "id": "page_background_position",
      "options": [
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "top",
          "label": "Top"
        },
        {
          "value": "bottom",
          "label": "Bottom"
        },
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "center",
      "label": "Background Position"
    },
    {
      "type": "header",
      "content": "t:sections.all.padding.section_padding_heading"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 36
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 36
    }
  ]
}
{% endschema %}