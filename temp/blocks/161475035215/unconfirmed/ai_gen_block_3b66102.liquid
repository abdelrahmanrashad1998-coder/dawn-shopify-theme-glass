{% doc %}
  @prompt
    generate a media and text section with 252525 for the background color and the content inside will be in the following sequence
    Meet The Founder 
    paragraph (At the heart of Danutti lies Dana El Gendy — a designer who turns passion into art and believes that true beauty begins with emotion.
    What started as quiet fascination became a language of gold, texture, and light — where every creation carries not just craftsmanship, but energy and meaning.
    For Dana, jewelry is not about perfection, but feeling. Each piece reflects her strength, her patience, and the fire that never fades.
    Her hands shape stories and symbols of grace and power — art that adorns the body yet speaks to the soul.
    Danutti is her spirit — bold yet delicate, timeless yet alive.
    Through her work, Dana invites every woman to wear her emotion as her finest jewel.)
    image
    NOTE:
    give the image full screen width and full height of the image itself
    use Ovo font, align meet the founder at the center and give the image full width of the screen in the mobile preview
{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-media-text-{{ ai_gen_id }} {
    background-color: #252525;
    width: 100%;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 0;
  }

  .ai-media-text-container-{{ ai_gen_id }} {
    display: grid;
    grid-template-columns: 1fr 1fr;
    width: 100%;
    max-width: 100%;
    min-height: 100vh;
    align-items: stretch;
  }

  .ai-media-text-content-{{ ai_gen_id }} {
    padding: 60px 80px;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    text-align: center;
    background-color: #252525;
  }

  .ai-media-text-heading-{{ ai_gen_id }} {
    font-family: 'Ovo', serif;
    font-size: {{ block.settings.heading_size }}px;
    color: {{ block.settings.text_color }};
    margin: 0 0 30px 0;
    line-height: 1.2;
    font-weight: 400;
  }

  .ai-media-text-paragraph-{{ ai_gen_id }} {
    font-family: 'Ovo', serif;
    font-size: {{ block.settings.text_size }}px;
    color: {{ block.settings.text_color }};
    line-height: 1.6;
    margin: 0;
    max-width: 600px;
  }

  .ai-media-text-image-wrapper-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    min-height: 100vh;
    position: relative;
    overflow: hidden;
  }

  .ai-media-text-image-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  .ai-media-text-image-placeholder-{{ ai_gen_id }} {
    width: 100%;
    height: 100%;
    min-height: 100vh;
    background-color: #f4f4f4;
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
  }

  .ai-media-text-image-placeholder-{{ ai_gen_id }} svg {
    width: 100%;
    height: 100%;
    max-width: 300px;
    max-height: 300px;
    opacity: 0.3;
  }

  .ai-media-text-empty-state-{{ ai_gen_id }} {
    position: absolute;
    bottom: 40px;
    left: 50%;
    transform: translateX(-50%);
    padding: 12px 20px;
    font-size: 14px;
    color: #666;
    text-align: center;
    pointer-events: none;
    background-color: rgba(255, 255, 255, 0.9);
    border-radius: 4px;
  }

  @media screen and (max-width: 749px) {
    .ai-media-text-container-{{ ai_gen_id }} {
      grid-template-columns: 1fr;
      grid-template-rows: auto 1fr;
    }

    .ai-media-text-content-{{ ai_gen_id }} {
      padding: 40px 20px;
      order: 1;
    }

    .ai-media-text-image-wrapper-{{ ai_gen_id }} {
      order: 2;
      min-height: 50vh;
    }

    .ai-media-text-image-placeholder-{{ ai_gen_id }} {
      min-height: 50vh;
    }

    .ai-media-text-heading-{{ ai_gen_id }} {
      font-size: {{ block.settings.heading_size | times: 0.8 }}px;
      margin-bottom: 20px;
    }

    .ai-media-text-paragraph-{{ ai_gen_id }} {
      font-size: {{ block.settings.text_size | times: 0.9 }}px;
    }
  }
{% endstyle %}

<div class="ai-media-text-{{ ai_gen_id }}" {{ block.shopify_attributes }}>
  <div class="ai-media-text-container-{{ ai_gen_id }}">
    <div class="ai-media-text-content-{{ ai_gen_id }}">
      {% if block.settings.heading != blank %}
        <h2 class="ai-media-text-heading-{{ ai_gen_id }}">{{ block.settings.heading }}</h2>
      {% endif %}
      
      {% if block.settings.paragraph != blank %}
        <div class="ai-media-text-paragraph-{{ ai_gen_id }}">{{ block.settings.paragraph }}</div>
      {% endif %}
    </div>

    <div class="ai-media-text-image-wrapper-{{ ai_gen_id }}">
      {% if block.settings.image %}
        <img
          src="{{ block.settings.image | image_url: width: 2000 }}"
          alt="{{ block.settings.image.alt | escape }}"
          loading="lazy"
          width="{{ block.settings.image.width }}"
          height="{{ block.settings.image.height }}"
          class="ai-media-text-image-{{ ai_gen_id }}"
        >
      {% else %}
        <div class="ai-media-text-image-placeholder-{{ ai_gen_id }}">
          {{ 'image' | placeholder_svg_tag }}
          <div class="ai-media-text-empty-state-{{ ai_gen_id }}">
            Add an image to complete the founder story
          </div>
        </div>
      {% endif %}
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Meet the founder",
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Meet The Founder"
    },
    {
      "type": "richtext",
      "id": "paragraph",
      "label": "Paragraph",
      "default": "<p>At the heart of Danutti lies Dana El Gendy — a designer who turns passion into art and believes that true beauty begins with emotion. What started as quiet fascination became a language of gold, texture, and light — where every creation carries not just craftsmanship, but energy and meaning. For Dana, jewelry is not about perfection, but feeling. Each piece reflects her strength, her patience, and the fire that never fades. Her hands shape stories and symbols of grace and power — art that adorns the body yet speaks to the soul. Danutti is her spirit — bold yet delicate, timeless yet alive. Through her work, Dana invites every woman to wear her emotion as her finest jewel.</p>"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Founder image"
    },
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "color",
      "id": "text_color",
      "label": "Text color",
      "default": "#ffffff"
    },
    {
      "type": "range",
      "id": "heading_size",
      "min": 24,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Heading size",
      "default": 36
    },
    {
      "type": "range",
      "id": "text_size",
      "min": 14,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Text size",
      "default": 16
    }
  ],
  "presets": [
    {
      "name": "Meet the founder"
    }
  ]
}
{% endschema %}