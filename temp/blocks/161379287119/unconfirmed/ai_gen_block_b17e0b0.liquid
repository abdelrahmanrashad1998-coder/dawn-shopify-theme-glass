{% doc %}
  @prompt
    generate a media and text section with 252525 for the background color and the content inside will be in the following sequence
    Meet The Founder 
    paragraph (At the heart of Danutti lies Dana El Gendy — a designer who turns passion into art and believes that true beauty begins with emotion.
    What started as quiet fascination became a language of gold, texture, and light — where every creation carries not just craftsmanship, but energy and meaning.
    For Dana, jewelry is not about perfection, but feeling. Each piece reflects her strength, her patience, and the fire that never fades.
    Her hands shape stories and symbols of grace and power — art that adorns the body yet speaks to the soul.
    Danutti is her spirit — bold yet delicate, timeless yet alive.
    Through her work, Dana invites every woman to wear her emotion as her finest jewel.)
    image
    NOTE:
    give the image full screen width and full height of the image itself
    use Ovo font, align meet the founder at the center and give the image full width of the screen in the mobile preview
{% enddoc %}
{% assign ai_gen_id = block.id | replace: '_', '' | downcase %}

{% style %}
  .ai-founder-section-{{ ai_gen_id }} {
    background-color: #252525;
    padding: 60px 0;
    width: 100%;
  }

  .ai-founder-container-{{ ai_gen_id }} {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 20px;
  }

  .ai-founder-content-{{ ai_gen_id }} {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 60px;
    align-items: center;
  }

  .ai-founder-text-{{ ai_gen_id }} {
    color: #ffffff;
  }

  .ai-founder-heading-{{ ai_gen_id }} {
    font-family: 'Ovo', serif;
    font-size: {{ block.settings.heading_size }}px;
    margin: 0 0 30px;
    text-align: center;
    font-weight: 400;
    line-height: 1.2;
  }

  .ai-founder-paragraph-{{ ai_gen_id }} {
    font-family: 'Ovo', serif;
    font-size: {{ block.settings.text_size }}px;
    line-height: 1.6;
    margin: 0;
    text-align: {{ block.settings.text_alignment }};
  }

  .ai-founder-image-wrapper-{{ ai_gen_id }} {
    width: 100%;
    height: auto;
  }

  .ai-founder-image-{{ ai_gen_id }} {
    width: 100%;
    height: auto;
    display: block;
    border-radius: {{ block.settings.image_border_radius }}px;
  }

  .ai-founder-image-placeholder-{{ ai_gen_id }} {
    width: 100%;
    min-height: 400px;
    background-color: #f4f4f4;
    display: flex;
    align-items: center;
    justify-content: center;
    border-radius: {{ block.settings.image_border_radius }}px;
    position: relative;
  }

  .ai-founder-image-placeholder-{{ ai_gen_id }} svg {
    width: 100%;
    height: 100%;
    max-width: 300px;
    max-height: 300px;
    opacity: 0.5;
  }

  .ai-founder-empty-state-{{ ai_gen_id }} {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%);
    padding: 12px 20px;
    background-color: rgba(0, 0, 0, 0.7);
    color: white;
    font-size: 14px;
    border-radius: 4px;
    text-align: center;
    pointer-events: none;
  }

  @media screen and (max-width: 749px) {
    .ai-founder-content-{{ ai_gen_id }} {
      grid-template-columns: 1fr;
      gap: 40px;
    }

    .ai-founder-section-{{ ai_gen_id }} {
      padding: 40px 0;
    }

    .ai-founder-container-{{ ai_gen_id }} {
      padding: 0 16px;
    }

    .ai-founder-heading-{{ ai_gen_id }} {
      font-size: {{ block.settings.heading_size | times: 0.8 }}px;
      margin-bottom: 20px;
    }

    .ai-founder-paragraph-{{ ai_gen_id }} {
      font-size: {{ block.settings.text_size | times: 0.9 }}px;
    }

    .ai-founder-image-wrapper-{{ ai_gen_id }} {
      width: 100vw;
      margin-left: calc(-50vw + 50%);
    }

    .ai-founder-image-{{ ai_gen_id }} {
      border-radius: 0;
    }

    .ai-founder-image-placeholder-{{ ai_gen_id }} {
      border-radius: 0;
      min-height: 300px;
    }
  }
{% endstyle %}

<div class="ai-founder-section-{{ ai_gen_id }}" {{ block.shopify_attributes }}>
  <div class="ai-founder-container-{{ ai_gen_id }}">
    <div class="ai-founder-content-{{ ai_gen_id }}">
      <div class="ai-founder-text-{{ ai_gen_id }}">
        {% if block.settings.heading != blank %}
          <h2 class="ai-founder-heading-{{ ai_gen_id }}">{{ block.settings.heading }}</h2>
        {% endif %}
        
        {% if block.settings.paragraph != blank %}
          <div class="ai-founder-paragraph-{{ ai_gen_id }}">{{ block.settings.paragraph }}</div>
        {% endif %}
      </div>

      <div class="ai-founder-image-wrapper-{{ ai_gen_id }}">
        {% if block.settings.image %}
          <img
            src="{{ block.settings.image | image_url: width: 800 }}"
            alt="{{ block.settings.image.alt | escape }}"
            loading="lazy"
            width="{{ block.settings.image.width }}"
            height="{{ block.settings.image.height }}"
            class="ai-founder-image-{{ ai_gen_id }}"
          >
        {% else %}
          <div class="ai-founder-image-placeholder-{{ ai_gen_id }}">
            {{ 'image' | placeholder_svg_tag }}
            <div class="ai-founder-empty-state-{{ ai_gen_id }}">
              Add an image of the founder
            </div>
          </div>
        {% endif %}
      </div>
    </div>
  </div>
</div>

{% schema %}
{
  "name": "Meet the founder",
  "tag": null,
  "settings": [
    {
      "type": "header",
      "content": "Content"
    },
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Meet The Founder"
    },
    {
      "type": "richtext",
      "id": "paragraph",
      "label": "Paragraph",
      "default": "<p>At the heart of Danutti lies Dana El Gendy — a designer who turns passion into art and believes that true beauty begins with emotion. What started as quiet fascination became a language of gold, texture, and light — where every creation carries not just craftsmanship, but energy and meaning. For Dana, jewelry is not about perfection, but feeling. Each piece reflects her strength, her patience, and the fire that never fades. Her hands shape stories and symbols of grace and power — art that adorns the body yet speaks to the soul. Danutti is her spirit — bold yet delicate, timeless yet alive. Through her work, Dana invites every woman to wear her emotion as her finest jewel.</p>"
    },
    {
      "type": "image_picker",
      "id": "image",
      "label": "Founder image"
    },
    {
      "type": "header",
      "content": "Style"
    },
    {
      "type": "range",
      "id": "heading_size",
      "min": 24,
      "max": 60,
      "step": 2,
      "unit": "px",
      "label": "Heading size",
      "default": 36
    },
    {
      "type": "range",
      "id": "text_size",
      "min": 14,
      "max": 24,
      "step": 1,
      "unit": "px",
      "label": "Text size",
      "default": 16
    },
    {
      "type": "select",
      "id": "text_alignment",
      "label": "Text alignment",
      "options": [
        {
          "value": "left",
          "label": "Left"
        },
        {
          "value": "center",
          "label": "Center"
        },
        {
          "value": "right",
          "label": "Right"
        }
      ],
      "default": "left"
    },
    {
      "type": "range",
      "id": "image_border_radius",
      "min": 0,
      "max": 40,
      "step": 2,
      "unit": "px",
      "label": "Image border radius",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "Meet the founder"
    }
  ]
}
{% endschema %}